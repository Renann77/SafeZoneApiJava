# Oracle DB
spring.datasource.url=jdbc:thin:@//oracle.fiap.com.br:1521/ORCL
spring.datasource.username=rm557820
spring.datasource.password=211005

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# Porta da aplicação (opcional)
server.port=8080

# Swagger / OpenAPI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html  # para garantir o path clássico

# JWT (exemplo de configuração, ajuste conforme necessário)
jwt.secret=chave-secreta-safezone-chave-com-mais-de-32-caracteres
jwt.expiration=7200000  # 2 horas em milissegundos
